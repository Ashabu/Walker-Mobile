import React from 'react';
import {
  SafeAreaView,
  ScrollView,
  StatusBar,
  StyleSheet,
  Text,
  useColorScheme,
  View,
} from 'react-native';
import AuthScreen from './Screens/AuthScreen';



const App = () => {

  return (
    <AuthScreen/>
  );
};

const styles = StyleSheet.create({

});

export default App;
